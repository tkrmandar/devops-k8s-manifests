apiVersion: v1
kind: Service
metadata:
  name: task-manager-api-green-svc
  namespace: prod
  labels:
    app: task-manager-api
    environment: prod
    slot: green
spec:
  selector:
    app: task-manager-api
    slot: green
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 3000
  type: ClusterIP
